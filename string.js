// Escaping \

var person = 'My name is \'John\'';
document.write(person);

// Operators -- arithmetic: +*-/% ++ -- 
        // -- assignment: = += Add and assign -= *= /= %= 
        // -- boolean: = && And || OR ! NOT 
        // -- comparison: = == And === SAME TYPE != NOT Equal !== not identical < > >= <= 
        // -- string: = = + +=Concatenate and assign
        // Ternary > :